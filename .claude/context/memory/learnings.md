# Learnings

- JusticePath uses a calm editorial legal design system: paper surfaces, espresso text, sun/sand accents, thin borders, and restrained motion.
- Existing reusable chat input components live under `src/components/prompt-kit`.
- Dashboard verification passed through lint, production build, and localhost HTTP 200 response.
